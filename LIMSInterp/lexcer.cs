namespace LIMSInterp;

public class lexcer
{
    private string[,] exp_tokens()
    {
        return new string[1,1];
    }

    public void lexcer(string characthers, string[,] exp_tokens)
    {
        int pos = 0;
        string[,] token;
        while (pos < characthers.Length)
        {
            
        }
    }
    
}